/**
 * Infrastructure layer — contains adapters for databases, external APIs, messaging,
 * and framework-specific configurations. Implements ports defined in the application layer.
 */
package com.agentmemorystore.infrastructure;
