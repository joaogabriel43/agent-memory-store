/**
 * Presentation layer — contains REST controllers, DTOs, and request/response mappings.
 * Handles HTTP concerns and delegates to the application layer.
 */
package com.agentmemorystore.presentation;
